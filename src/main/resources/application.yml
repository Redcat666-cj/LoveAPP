demo:
  runner:
    enabled: false

spring:
  application:
    name: yu-ai-agent
  profiles:
    active: local
#  datasource:
#    url: jdbc:postgresql://pgm-bp11he7d67v7b4g7so.rwlb.rds.aliyuncs.com:5432/zhiqing_ai_agent
#    username: my_user
#    password: "061214cJ"
#  ai:
#    vectorstore:
#     pgvector:
#      index-type: HNSW
#      distance-type: L2_DISTANCE
#      dimensions: 1024
#      max-document-batch-size: 10000 # Optional: Maximum number of documents per batch
    mcp:
      client:
       stdio:
       servers-configuration: "classpath:mcp-server.json"
#        sse:
#         connections:
#           server1:
#              url: http://localhost:8127


server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi 接口文档的配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.itzhiqin.yuaiagent.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
# searchAPI key
search-api:
  api-key: CdoM1N2eaYoTQYh92YnnjL7z
kind: Application